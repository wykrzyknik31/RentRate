# RentRate Environment Variables
# Copy this file to .env and customize as needed

# Database Configuration (PostgreSQL)
POSTGRES_USER=rentrate
POSTGRES_PASSWORD=rentrate
POSTGRES_DB=rentrate

# Backend Configuration
FLASK_ENV=development
SECRET_KEY=dev-secret-key-change-in-production

# Translation Service Configuration
# Default: Public LibreTranslate API (https://libretranslate.com)
# Note: Public API has rate limits. For production use, consider:
#   1. Getting a LibreTranslate API key (free tier available)
#   2. Self-hosting LibreTranslate (see docker-compose.yml for instructions)
LIBRETRANSLATE_URL=https://libretranslate.com
LIBRETRANSLATE_API_KEY=

# Frontend Configuration
NEXT_PUBLIC_API_URL=http://localhost:5000
